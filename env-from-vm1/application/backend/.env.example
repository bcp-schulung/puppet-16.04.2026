# Copy this file to .env and fill in your values
PORT=3001

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=jokes

# Restrict CORS to the frontend origin (optional, defaults to *)
FRONTEND_ORIGIN=http://localhost:3000
