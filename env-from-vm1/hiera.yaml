---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Node-specific data"
    path: "nodes/%{trusted.certname}.yaml"
#  - name: "Role-specific data"
#    glob: "roles/*.yaml"
#    #TODO: path: "role/%{facts.role}.yaml"
  - name: "Common data"
    path: "common.yaml"
